function init() {
	// SLIDE SHOW PLANS
	// change styling so sections fit to screen height
	// ?? hide overflow so only one section at a time shows
	// add left and right slideshow nav buttons
	// add event listener for buttons to move viewable content
}

window.onload = init;
